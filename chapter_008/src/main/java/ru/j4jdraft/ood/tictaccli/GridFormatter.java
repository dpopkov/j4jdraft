package ru.j4jdraft.ood.tictaccli;

/**
 * Formatter that translates a grid into string representation.
 */
public interface GridFormatter {
    /** Formats the specified grid into String. */
    String format(GridView gridView);
}
