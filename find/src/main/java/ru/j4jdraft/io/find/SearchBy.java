package ru.j4jdraft.io.find;

/** Type of search. */
public enum SearchBy {
    /** Search by mask. */
    MASK,
    /** Search by full name. */
    FULL,
    /** Search using regular expression. */
    REGEX
}
